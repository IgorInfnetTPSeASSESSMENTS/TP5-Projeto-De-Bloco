package adopet.selenium.pages.components;

import adopet.selenium.base.BasePage;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class MessagesFragment extends BasePage {
        public MessagesFragment(WebDriver driver) {
        super(driver);
    }

}